{#- Product route — MagicFront flow (Motor A): the 'mff_PRODUCT' singleton template
    is painted for every product via the shared widget-blob content template, exactly
    like Home. The native LC design-assets engine (design snippets) is NOT used here.
    Phase 2 adds the declarative product data binding on top of this same blob. -#}
{%- extends 'overrideContent/_shared/mff-page-content.html.twig' -%}
