{#- Category route — MagicFront flow (Motor A): the 'mff_CATEGORY' singleton template
    is painted for every category via the shared widget-blob content template, exactly
    like Home and Product. The native LC category design-assets engine (Motor B) is NOT
    used here. -#}
{%- extends 'overrideContent/_shared/mff-page-content.html.twig' -%}
